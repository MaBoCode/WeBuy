<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view"
    >

    <item android:id="@+id/promotions_nav_item"
        android:icon="@drawable/ic_home"
        android:title="@string/promotions_title"
        />

    <item android:id="@+id/shops_nav_item"
        android:icon="@drawable/ic_store"
        android:title="@string/shops_title"
        />

    <item android:id="@+id/cart_nav_item"
        android:icon="@drawable/ic_shopping_cart"
        android:title="@string/shopping_cart_title"
        />

    <item android:id="@+id/profile_nav_item"
        android:icon="@drawable/ic_profile"
        android:title="@string/profile_title"
        />

    <item android:id="@+id/settings_nav_item"
        android:icon="@drawable/ic_settings"
        android:title="@string/settings_title"
        />

</menu>